To test locally use:
```
gem install jekyll
bundle install
npm install
bundle exec jekyll build
npm run pagefind
bundle exec jekyll serve
```

Pagefind will index the built site in `_site/` and create the search index in `_site/pagefind/`.
